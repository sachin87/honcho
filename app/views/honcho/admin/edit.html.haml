%h1 
  = "Editing #{resource_name.singularize}"
= render 'form'
= link_to 'Show', honcho.send("#{singularize_resource}_path",@resource.id)
|
= link_to 'Back', honcho.send("#{resource_name.downcase}_path")