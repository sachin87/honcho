- f.object.class.table_attributes.each do |attribute|
  .row
    .small-3.columns
      = f.label attribute, *label_options(attribute, f)
    .small-9.columns
      = f.input attribute.to_sym, input_options(f, attribute)
= f.button :submit, class: 'ui primary button'